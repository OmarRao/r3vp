# Template — encrypt this file with: sops --encrypt --age <recipient-pubkey> secrets.yaml > secrets.enc.yaml
veeam:
  base_url: "https://veeam-server:9419"
  username: "administrator"
  password: "CHANGE_ME"

vcenter:
  host: "vcenter.example.local"
  username: "administrator@vsphere.local"
  password: "CHANGE_ME"
